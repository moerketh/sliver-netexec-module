name: Test

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.12'

    - name: Install dependencies with pipx
      run: |
        pipx install git+https://github.com/Pennyw0rth/NetExec
        pipx inject netexec sliver-py
        pipx runpip netexec install pytest flake8

    - name: Run tests
      run: |
        VENV_PATH=$(pipx environment netexec --path)
        "$VENV_PATH/bin/python" -m pytest tests/test_sliver_exec.py -v
